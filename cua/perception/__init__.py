"""Turning a raw surface into a normalized element graph.

The in-page half lives in extract.js; the Python half is the surface
implementation that runs it per frame and assembles the observation.
"""
